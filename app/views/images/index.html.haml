%p#notice= notice

%h1 Listing Images

= link_to 'Post Image', new_image_path

%table
  %thead
    %tr
      %th Image
      %th Comment
      %th{colspan: 3}
  %tbody
    - @images.each do |image|
      %tr
        %td= image.image_url
        %td= image.comment
        %td= link_to 'Show', image
        %td= link_to 'Edit', edit_image_path(image)
        %td= link_to 'Destroy', image, method: :delete, data: { confirm: 'Sure?' }
